/**
 * Screen layout constants – single place for content padding and spacing.
 * Change these to adjust gap below header / above content across the app.
 */

/** Horizontal padding for main content (left/right). */
export const CONTENT_PADDING_HORIZONTAL = 24;

/** Extra gap below the tab/stack header (on top of safe area insets). */
export const CONTENT_PADDING_TOP_EXTRA = 16;

/** Extra padding at bottom of scroll content. */
export const CONTENT_PADDING_BOTTOM_EXTRA = 24;

/** For stack screens that already have a header: top padding only (no safe area). */
export const STACK_CONTENT_PADDING_TOP = 16;
